# This file exists only to ensure the data/ directory is tracked by Git.
# tasks.json is gitignored (it is created automatically when the app runs).
# Each user who clones this project gets a fresh, empty task list.
